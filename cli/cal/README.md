# Mic calibration files

Measurement-mic frequency-response curves for `XAir_ToastSaver --mic-cal`.

- `24161.txt` — factory cal (256-pt, ref 1 kHz @ -38.6 dBFS).

Usage: `XAir_ToastSaver -i <ip> --mic-cal cli/cal/24161.txt -c <mic-ch>`
