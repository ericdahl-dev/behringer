---
id: T-022
title: "Ring-out: monitor gain drive, target/RTA selection, panic restore"
status: done
assignee: null
priority: high
tags: [toast-saver, ring-out, osc, safety]
depends_on: [T-013]
created_at: 2026-06-29
updated_at: 2026-06-29
---

## Goal

Add the **action-executor plumbing** the ring-out controller drives: ramp a
chosen monitor bus fader over OSC, ensure the RTA taps that monitor, insert the
notch TEQ on the bus under test, and a **panic restore** that snaps gain back to
its start value. This is the only platform-specific half of the feature.

See [`docs/ringout-design.md`](../docs/ringout-design.md) ("Targeting", "Safety").

## OSC paths (XAir / XR18, port 10024)

```
/bus/{N}/mix/fader     ,f   {0.0..1.0}   // drive monitor gain (see cheat-sheet float↔dB)
/bus/{N}/insert/sel    ,i   {0..4}       // insert FX slot on the monitor bus (OFF,Fx1..Fx4)
/fx/{slot}/par/{nn}    ,f   {0.0..1.0}   // existing TEQ notch (send_teq_band)
/meters/4              (blob)            // RTA source — must tap the monitor mic
```

Fader float↔dB conversion: use the piecewise formula in
[`docs/xr18-xair-osc-cheatsheet.md`](../docs/xr18-xair-osc-cheatsheet.md).

## Acceptance Criteria

- [x] New args: `--ringout-bus N` (target monitor bus), `--ringout-ceiling dB`,
      `--ringout-step dB`, `--ringout-margin dB`, `--ringout-max-notches N`.
      (Plus a `--ringout` mode flag; parsed via `getopt_long`.)
- [x] `ringout_set_gain(AppState*, float dB)` — converts dB→float (shared
      cheat-sheet formula, `fader_db_to_float` in `toast_logic`) and sends
      `/bus/N/mix/fader`; tracks current driven gain in `AppState`.
- [x] On ring-out start: record the bus's **original fader** value (via
      `osc_query_float`); ensure the FX-slot TEQ is inserted on the target bus
      (`/bus/N/insert/sel` + `/bus/N/insert/on`); print a one-line confirmation
      of bus, FX slot, ceiling, and start gain.
- [x] `ringout_panic(AppState*)` — immediately restores the bus fader to the
      recorded original value; reached on **SIGINT (Ctrl-C)** via the hold-loop
      exit and callable on `RO_ABORT` (T-023). Logs the restored value.
- [x] RTA-tap guard: logs the configured RTA source channel and a reminder to
      ensure it taps the target monitor mic before ramping (best-effort warning;
      auto-select deferred).
- [x] Reuses existing UDP send + `send_teq_band`; no change to reactive behaviour
      (ring-out is a separate `run_ringout` flow gated on `--ringout`).

## Progress

2026-06-29: Added the pure XAir fader value encoding `fader_db_to_float` /
`fader_float_to_db` to `toast_logic.{c,h}` (piecewise cheat-sheet formula) with
15 host asserts in `test_toast_logic.c` (suite 30→45). In `XAir_ToastSaver.c`:
ring-out config + state in `AppState`; `osc_query_float` (read current fader
before subscribing to meters); `ringout_set_gain` (dB→fader, **belt-and-suspenders
clamp to ceiling**); `ringout_panic` (restore captured fader); `run_ringout`
(capture original fader → refuse if unreadable, insert TEQ on the bus, subscribe
RTA, assert start gain, then hold until Ctrl-C and restore). `getopt_long` adds
`--ringout[-bus|-ceiling|-step|-margin|-max-notches]` with validation
(bus 1-6, step > 0, max-notches ≥ 1). The control/ramp loop is deliberately left
to T-023 — this proves gain-drive, bus-insert, and panic-restore plumbing.
Verified: `make all` clean (no `-Wall` warnings); `make check` 45/16/22 green;
arg validation + handshake smoke-tested. Real-mixer/emulator validation is T-025.

## Notes

- Gain drive is the dangerous bit — the **ceiling is enforced in `ringout_step`
  (T-021)**; this layer additionally clamps any commanded dB to `ceiling_db` as a
  belt-and-suspenders guard before sending.
- Keep the original-fader restore robust to early exit (atexit / signal handler).
